name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  checks: write
  security-events: write

env:
  GO_VERSION: "1.25"

jobs:
  setup:
    name: Setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: hl/go.sum

      - name: Build hl
        run: go build -o "$GITHUB_WORKSPACE/bin/hl" .
        working-directory: hl

      - name: Setup tools
        run: |
          "$GITHUB_WORKSPACE/bin/hl" build setup

      - name: Upload toolchain
        uses: actions/upload-artifact@v4
        with:
          name: toolchain
          path: bin/
          retention-days: 1

  ci:
    name: CI
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: ${{ env.GO_VERSION }}
          cache-dependency-path: hl/go.sum

      - name: Download toolchain
        uses: actions/download-artifact@v4
        with:
          name: toolchain
          path: bin/

      - name: Prepare tools
        run: |
          chmod +x bin/*
          echo "$GITHUB_WORKSPACE/bin" >> "$GITHUB_PATH"

      - name: Check
        run: hl ci check
        env:
          CI: "true"

      - name: Upload test reports
        if: always()
        uses: dorny/test-reporter@v1
        with:
          name: Go Tests
          path: test-results/*.xml
          reporter: java-junit
          fail-on-error: false

      - name: Log in to Docker Hub
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build images (PR)
        if: github.event_name == 'pull_request'
        run: |
          hl ci docker \
            --changed \
            --base "origin/${{ github.base_ref }}" \
            --tag "${{ github.sha }}"
        env:
          CI: "true"

      - name: Build and push images (main)
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          hl ci docker \
            --changed \
            --base "${{ github.event.before }}" \
            --push \
            --tag latest \
            --tag "${{ github.sha }}"
        env:
          CI: "true"

  security:
    name: Security
    needs: setup
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download toolchain
        uses: actions/download-artifact@v4
        with:
          name: toolchain
          path: bin/

      - name: Prepare tools
        run: |
          chmod +x bin/*
          echo "$GITHUB_WORKSPACE/bin" >> "$GITHUB_PATH"

      - name: golangci-lint (SARIF)
        if: always()
        run: |
          mkdir -p "$GITHUB_WORKSPACE/sarif"
          for mod in $(find . -name go.mod -not -path '*/vendor/*' | xargs -I{} dirname {}); do
            name=$(echo "$mod" | tr '/./' '-' | sed 's/^-//')
            echo "==> Linting $mod"
            (cd "$mod" && golangci-lint run --output.sarif.path="$GITHUB_WORKSPACE/sarif/golangci-${name}.sarif" ./...) || true
          done
        working-directory: .

      - name: Gitleaks (SARIF)
        if: always()
        run: |
          gitleaks dir . --report-format sarif --report-path "$GITHUB_WORKSPACE/sarif/gitleaks.sarif" || true

      - name: SBOM generation (syft)
        if: always()
        run: |
          mkdir -p "$GITHUB_WORKSPACE/sbom"
          for mod in $(find . -name go.mod -not -path '*/vendor/*' | xargs -I{} dirname {}); do
            name=$(echo "$mod" | tr '/./' '-' | sed 's/^-//')
            echo "==> SBOM for $mod"
            syft scan "dir:$mod" -o spdx-json="$GITHUB_WORKSPACE/sbom/${name}.spdx.json" -o cyclonedx-json="$GITHUB_WORKSPACE/sbom/${name}.cdx.json"
          done

      - name: Vulnerability scan (grype)
        if: always()
        run: |
          for sbom in "$GITHUB_WORKSPACE"/sbom/*.spdx.json; do
            [ -f "$sbom" ] || continue
            name=$(basename "$sbom" .spdx.json)
            echo "==> Scanning $name"
            grype "sbom:$sbom" --output sarif --file "$GITHUB_WORKSPACE/sarif/grype-${name}.sarif" || true
          done

      - name: Clean empty SARIF files
        if: always()
        run: find "$GITHUB_WORKSPACE/sarif" -name '*.sarif' -empty -delete

      - name: Upload SARIF reports
        if: always()
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: sarif/

      - name: Upload SBOMs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: sbom
          path: sbom/
