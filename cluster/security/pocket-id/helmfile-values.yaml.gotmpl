ingress:
  enabled: true
  host: "auth.{{ .Environment.Values.domain }}"
  tls:
    enabled: true

env:
  # Traefik serves the Let's Encrypt wildcard certificate for auth.<domain>.
  PUBLIC_APP_URL: "https://auth.{{ .Environment.Values.domain }}"
