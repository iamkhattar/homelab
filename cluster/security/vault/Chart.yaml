apiVersion: v2
name: vault
description: A Helm chart to deploy HashiCorp Vault for the homelab k3s cluster
type: application
version: 0.1.0
appVersion: "1.18.0"
dependencies:
  - name: vault
    version: "0.30.0"
    repository: https://helm.releases.hashicorp.com
