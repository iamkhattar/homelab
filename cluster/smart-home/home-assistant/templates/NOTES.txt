Home Assistant is available at https://{{ .Values.ingress.host }}.

Complete native owner enrollment, enable MFA, then configure the MQTT
integration only after Mosquitto and its Vault-managed credentials are Ready.
