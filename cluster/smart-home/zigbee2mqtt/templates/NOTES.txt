Zigbee2MQTT is available at https://{{ .Values.ingress.host }}.

Coordinator: {{ .Values.coordinator.device }} ({{ .Values.coordinator.adapter }})
The workload is pinned to the labelled Titan node and is the only process that
may open this coordinator. Do not also configure Home Assistant ZHA.
You may need to use a nodeSelector to pin this pod to the node with the USB adapter.
